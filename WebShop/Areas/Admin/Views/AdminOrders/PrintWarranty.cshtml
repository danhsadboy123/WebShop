@using Microsoft.AspNetCore.Html;
@model WebShop.Models.Order
@{

}
@if (ViewBag.BBBG != null && ViewBag.BBBG != "<p><br></p>")
{
    @(new HtmlString(ViewBag.BBBG.ToString()));
}
@if (ViewBag.WarrantyCon != null && ViewBag.WarrantyCon != "<p><br></p>")
{
    @(new HtmlString(ViewBag.WarrantyCon.ToString()));
}
