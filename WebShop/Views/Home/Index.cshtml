@inject WebShop.Services.IPageInfoService PageInfoService;
@{

    // List<WebShop.Models.Category> lsCat = ViewBag.lsCat;
    // ViewData["Title"] = @PageInfoService.GetPageInfo().Title;
    // ViewData["MoTa"] = @PageInfoService.GetPageInfo().MetaDesc;
    // ViewData["Keywords"] = @PageInfoService.GetPageInfo().MetaKey;
 
    // ViewData["OGimage"] = PageInfoService.GetPageInfo().Domain + "Adminassets/images/logo/" + PageInfoService.GetPageInfo().OgImage;
     
} 
@section Styles{
    <style>
        .owl-item KichHoat {
            width: 200px !important;
            margin-right: 29px !important;
        } 
    </style>
} 
@await Html.PartialAsync("~/Views/Shared/_CarouselPartialView.cshtml")

<!-- Featured Start -->
<div class="container-fluid pt-2">
    <div class="row px-xl-5 pb-3">
        <div class="col-lg-3 col-md-6 col-sm-12 pb-1">
            <div class="d-flex align-items-center bg-light mb-4" style="padding: 30px;">
                <h1 class="fas fa-check text-primary m-0 mr-3"></h1>
                <h5 class="font-weight-semi-bold m-0">Sản phẩm chất lượng</h5>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-12 pb-1">
            <div class="d-flex align-items-center bg-light mb-4" style="padding: 30px;">
                <h1 class="fas fa-shipping-fast text-primary m-0 mr-2"></h1>
                <h5 class="font-weight-semi-bold m-0">Miễn phí giao hàng</h5>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-12 pb-1">
            <div class="d-flex align-items-center bg-light mb-4" style="padding: 30px;">
                <h1 class="fas fa-exchange-alt text-primary m-0 mr-3"></h1>
                <h5 class="font-weight-semi-bold m-0">14 ngày đổi trả</h5>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-12 pb-1">
            <div class="d-flex align-items-center bg-light mb-4" style="padding: 30px;">
                <h1 class="fas fa-SoDienThoai-volume text-primary m-0 mr-3"></h1>
                <h5 class="font-weight-semi-bold m-0">Hỗ trợ 24/7 </h5>
            </div>
        </div>
    </div>
</div>
<!-- Featured End -->
<!-- Categories Start -->
<div class="container-fluid pt-2">
    <h2 class="section-title position-relative mx-xl-5 mb-4"><span class="  pr-3" style="background-color:#F0F1FB">Công nghệ toàn cầu</span></h2>
    <div class="row px-xl-5  ">
@*         @foreach (var item in lsCat)
        {
            <div class="col-lg-3 col-md-4 col-sm-6  p-1">
                <a class="text-decoration-none click-cate" href="/@item.Alias">
                    <div class="cat-item d-flex align-items-center mb-1">
                        <div class="overflow-hidden" style="width: 100px; height: 100px;">
                            <img class="img-fluid" src="@item.Thumb" alt="@item.Alias" loading="lazy">
                        </div>
                        <div class="flex-fill pl-3">
                            <h6>@item.CatName</h6>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div> *@
</div>
<!-- Categories End -->
<!-- Products Start -->
<div class="container-fluid pt-2 pb-3 ">
    @foreach (var item in ViewBag.categoriesWithProducts)
    {
        <a href="/@item.category.Alias" class="">
            <h2 class="section-title position-relative mx-xl-5 mb-4 mt-5"><span class=" pr-3" style="background-color:#F0F1FB">@item.category.CatName</span></h2>
        </a>
        <div class="row px-xl-5" style="margin:0; padding:0;justify-content:center;"> 
            @if (item.Banner != null)
            {
                <div class="col-12 col-md-12 col-lg-3 poster">
                    <img src="~/@item.Banner.Banner1" class="img-fluid" alt="@item.Banner.Banner1" loading="lazy" style="height:100%;" />
                </div>
            }
            else
            {
                <div class="col-12 col-md-12 col-lg-3 poster">
                    <p>đang cập nhật</p>
                </div>

            }
            <div class="row justify-content-center row-category">
                @foreach (var product in item.lsProducts)
                {
                    <div class=" item-product-Cate  " title="Xem chi tiết @product.ProductName">
                        <div class="product-item bg-light mb-1" style="padding:15px">
                            <div class="product-img position-relative overflow-hidden">
                                <div class="d-flex justify-content-center" style="height:200px;">
                                    <img style="object-fit:contain;" class="img-fluid" src="https://novazone.co/@product.Avatar" alt="@product.Alias" loading="lazy">
                                </div>
                                <div class="product-action">
                                    @if (product.UnitsInStock > 0)
                                    {
                                        <a class="btn btn-outline-dark btn-square" href="javascript:void(0)" onclick="addToCart(@product.ProductId)"><i class="fa fa-shopping-cart"></i></a>
                                        <a class="btn btn-outline-dark btn-square" href="/@product.Alias-@product.ProductId"><i class="fa fa-ellipsis-h"></i></a>
                                    }
                                    else
                                    {
                                        <a class="btn btn-outline-dark btn-square" href="/@product.Alias-@product.ProductId"><i class="fa fa-bars"></i></a>
                                    }
                                </div>
                            </div>
                            <div class="text-center py-3">
                                <a class="text-decoration-none p-7 text-dark"
                                   style="height: 3.3em; display: block; overflow: hidden; line-height: 1.5em; white-space: normal; text-overflow: ellipsis; text-align:left; font-size:15px;font-weight: bold;"
                                   href="/@product.Alias-@product.ProductId">
                                    @product.ProductName
                                </a>

                                <div class="d-md-flex  justify-content-center mt-2 flex-column">
                                    @if (product.Price <= 0)
                                    {
                                        <div class="md-d-flex  " style="font-size: 13px;text-align:left;">
                                            <span class="text-muted" style="margin-right: 5px;"> </span>
                                            <span class="text-success ml-2 d-none d-lg-inline "></span>
                                        </div>
                                        <span class="d-block">Giá: <b class="text-danger">liên hệ</b></span>
                                    }
                                    else
                                    {
                                        @if (product.SalePrice != null && product.SalePrice < product.Price)
                                        {
                                            <div class="md-d-flex  " style="font-size: 13px;text-align:left;">
                                                <span class="text-muted" style="margin-right: 5px;"><del>@product.Price.ToString("N0")₫</del></span>
                                                <span class="text-success ml-2 d-none d-lg-inline ">Tiết kiệm @((product.Price - product.SalePrice)?.ToString("N0"))₫</span>
                                            </div>
                                            <span class="text-danger" style="font-size: 1.1em; font-weight: bold; text-align:left;">Giá: @product.SalePrice.ToString("N0")₫</span>
                                        }
                                        else
                                        {
                                            <div class="md-d-flex  " style="font-size: 13px;text-align:left;">
                                                <span class="text-muted" style="margin-right: 5px;"> </span>
                                                <span class="text-success ml-2 d-none d-lg-inline "> </span>
                                            </div>
                                            <span class="text-danger " style="font-size: 1.1em; font-weight: bold;text-align:left;">Giá: @product.Price.ToString("N0")₫</span>
                                        }
                                    }
                                </div>
                                <div class="mt-1 "style="text-align:left;">
                                    @if (product.UnitsInStock>0)
                                    {
                                        <span style="color: #2cc067; font-size: 14px;line-height: 30px;" class=""><i class="fa fa-check" style="color:#2cc067"></i> Sẵn hàng</span>
                                    }
                                    else
                                    {
                                        <span style="color:#0074da"><i class="fas fa-SoDienThoai fa-flip-horizontal"></i> Đặt hàng</span>
                                    } 
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>

        </div>
    }
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary btn-seeMoreProductHome">Xem thêm</button>
    </div>

</div>
<!-- Products End -->
<div class="container-fluid pb-3 categoriesWithProductsSeeMore" id="product-container">
</div>



<!-- Offer Start --> 
 
<div class="container-fluid pt-2 pb-3">
    <div class="row px-xl-5">
        @foreach (var item in ViewBag.discounts)
        {
            
            <div class="col-md-6">
                <div class="product-offer mb-30" style="height: 300px;">
                    <img class="img-fluid" src="~/@item.Image" alt="@item.Image" loading="lazy">
                    <div class="offer-text">
                        <h6 class="text-white text-uppercase">@item.MoTa</h6>
                        <h3 class="text-white mb-3">@item.Name</h3>
                        <!-- Sử dụng HashedId thay vì Id trong URL -->
                        <a href="/gian-hang-khuyen-mai/@item.Id" class="btn btn-primary">Xem ngay</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>



<!-- Offer End -->
<!-- Vendor Start -->
<div class="container-fluid  ">
    <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class=" pr-3" style="background-color:#F0F1FB">Đối tác của chúng tôi</span></h2>
    <div class="row px-xl-5">
        <div class="col">
            <div class="owl-carousel vendor-carousel">
                @foreach (var item in ViewBag.brand)
                {
                    <div class="bg-light p-4  ">
                        <img src="@item.Thumb  " loading="lazy" class="img-fluid" style="height:50px; width:100%;" alt="@item.BrandName">
                    </div>
                }
            </div>
        </div>
    </div>
</div>
<!-- Vendor End -->



